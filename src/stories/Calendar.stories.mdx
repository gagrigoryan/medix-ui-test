import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import { Calendar } from "../components";

<Meta title="Calendar" component={Calendar} />

export const Template = ({...args}) => {
    return (
        <Calendar {...args} />
    )
}

export const currentMonth = new Date().getMonth()

# Календарь

<Canvas>
    <Story name="Base">
        {Template.bind({})}
    </Story>
    <Story name="Multiple" args={{isMultiple: true}}>
        {Template.bind({})}
    </Story>
    <Story
        name="With Value"
        args={{
            value: {startDate: new Date(2023, currentMonth, 9)}
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="With Multiple Value"
        args={{
            value: {
                startDate: new Date(2023, currentMonth, 10),
                finishDate: new Date(2023, currentMonth, 20)
            },
            isMultiple: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Calendar} />

### Использование:
Для использования следует импортировать компонент `Calendar` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {Calendar} from "marketplace-ui-kit";
        ...
        <Calendar />
        <Calendar isMultiple />
        <Calendar value={{startDate: new Date(2023, currentMonth, 9)}} />
        <Calendar
            isMultiple
            value={{
                startDate: new Date(2023, currentMonth, 10),
                finishDate: new Date(2023, currentMonth, 20)
            }}
            onChange={({startDate, finishDate}) => {...}}
         />
  `}
/>
