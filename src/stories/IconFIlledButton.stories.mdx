import {Canvas, Meta, Story, Source, ArgsTable} from "@storybook/addon-docs/blocks";
import {IconFilledButton} from "../components";
import {StarIcon} from "../components/Icons";

<Meta
    title="Button/Icon Filled Button"
    component={IconFilledButton}
/>

export const Template = ({isLight, ...args}) => {
    return (
        <IconFilledButton icon={<StarIcon />} isLight={isLight} {...args}>Button</IconFilledButton>
    );
}

# Кнопка (Icon Filled)

<Canvas>
    <Story name="Base">
        {Template.bind({})}
    </Story>
    <Story name="Small" args={{isSmall: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Disabled" args={{isDisabled: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Disabled Small" args={{isSmall: true, isDisabled: true}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={IconFilledButton} />

### Использование:

Для использования следует импортировать компонент `IconFilledButton` из `marketplace-ui-kit`.
<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {IconFilledButton} from "marketplace-ui-kit";
        ...
        <IconFilledButton icon={<SomeIcon />}>Button</IconFilledButton>
        <IconFilledButton isSmall icon={<SomeIcon />}>Button</IconFilledButton>
        <IconFilledButton isDisabled icon={<SomeIcon />}>Button</IconFilledButton>
  `}
/>
