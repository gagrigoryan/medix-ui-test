import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import {InputArea, VisibleIcon} from "../components";

<Meta title="TextField/InputArea" component={InputArea} />

export const Template = (args) => {
    return (
        <div style={{width: "440px"}}>
            <InputArea {...args} />
        </div>
    );
}

# Текстовое поле

<Canvas isColumn={true}>
    <Story name="Base" args={{label: "Input Area label", placeholder: "Input Area text"}}>
        {Template.bind({})}
    </Story>
    <Story
        name="With Description"
        args={{label: "With Description", placeholder: "Input Area text", description: "Description"}}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Clearable"
        args={{label: "Clearable", placeholder: "Input Area text", description: "Description", isClearable: true}}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Clearable With Value"
        args={{label: "Clearable", placeholder: "Input Area text", description: "Description", isClearable: true, value: "Default value"}}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Disabled"
        args={{label: "Disabled", placeholder: "Input Area text", description: "Description", isDisabled: true}}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Error"
        args={{label: "Error", placeholder: "Input Area text", description: "Description", error: "Error message"}}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Required"
        args={{label: "Required", placeholder: "Input Area text", description: "Description", isRequired: true}}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={InputArea} />

### Использование:
Для использования следует импортировать компонент `InputArea` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {InputArea} from "marketplace-ui-kit";
        ...
        <InputArea label="Label" placeholder="Placeholder" />
        <InputArea label="Label" placeholder="Placeholder" description="Description" />
        <InputArea
            label="Label"
            placeholder="Placeholder"
            description="Description"
            isClearable
        />
        <InputArea
            label="Label"
            placeholder="Placeholder"
            description="Description"
            isDisabled
        />
        <InputArea
            label="Label"
            placeholder="Placeholder"
            description="Description"
            error="Error message"
        />
        <InputArea
            label="Label"
            placeholder="Placeholder"
            description="Description"
            isRequired
        />
  `}
/>
