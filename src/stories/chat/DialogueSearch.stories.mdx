import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import {EDialogueSearch, DialogueSearch} from "../../components";

<Meta title="Chat/Dialogue/DialogueSearch" component={DialogueSearch} />

export const Template = (args) => {
    return (
        <div style={{width: "34vw"}}>
            <DialogueSearch {...args} />
        </div>
    );
}

# Поисковая строка

<Canvas isColumn>
    <Story name="Base" args={{}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={DialogueSearch} />

### Использование:
Для использования следует импортировать компонент `DialogueSearch` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {DialogueSearch} from "marketplace-ui-kit";
        ...
        <DialogueSearch />
  `}
/>
