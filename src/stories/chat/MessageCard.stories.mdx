import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import { MessageCard } from "../../components"; import MessageCardWidget from "../components/MessageCardWidget";

<Meta title="Chat/Message/MessageCard" component={MessageCard} />


export const Template = (args) => {
    return (
        <MessageCardWidget {...args} />
    );
}

# Сообщение

<Canvas>
    <Story name="Base" args={{withUser: false, withReply: false, withDocument: false, withFile: false}}>
        {Template.bind({})}
    </Story>
    <Story name="With User" args={{withUser: true, withReply: false, withDocument: false, withFile: false}}>
        {Template.bind({})}
    </Story>
    <Story name="With Reply" args={{withUser: false, withReply: true, withDocument: false, withFile: false}}>
        {Template.bind({})}
    </Story>
    <Story name="With Document" args={{withUser: false, withReply: true, withDocument: true, withFile: false}}>
        {Template.bind({})}
    </Story>
    <Story name="With File" args={{withUser: false, withReply: true, withDocument: true, withFile: true}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={MessageCard} />

### Использование:
Для использования следует импортировать компонент `MessageCard` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {MessageCard, EMessageStatus} from "marketplace-ui-kit";
        const messageCardProps = {
            text: "...",
            time: "10:49 pm",
            status: EMessageStatus.Delivered,
            userModel: {
                name: "Alice Cuddy",
                post: "MD",
                image: "/avatar.jpg"
            },
            replyModel: {
                username: "Alice Cuddy",
                text: "..."
            },
            fileModel: {
                filename: "File with long very name 01.pdf",
                previewImage: "/previewImage.pdf"
            },
            documentModel: {
                 name: "Hepatitis C virus antibody test (ELISA)",
                 previewImage: "/previewImage.pdf",
                 date: "03.31.2022",
                 size: "3,2 MB"
            }
        }
        ...
        <MessageCard {...messageCardProps} />
        <MessageCard isIncoming {...messageCardProps} />
`}
/>
