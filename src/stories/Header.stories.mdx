import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import {Header} from "../components";

<Meta title="Widget/Header" component={Header} />

export const Template = (args) => {
    return (
        <div style={{minWidth: "1440px"}}>
            <Header {...args} />
        </div>
    );
}

# Шапка

<Canvas>
    <Story name="Base" args={{}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Header} />

### Использование:
Для использования следует импортировать компонент `Header` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {Header} from "marketplace-ui-kit";
        ...
        <Header />
  `}
/>
