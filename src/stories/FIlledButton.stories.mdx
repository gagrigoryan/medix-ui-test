import {Canvas, Meta, Story, Source, ArgsTable} from "@storybook/addon-docs/blocks";
import {FilledButton} from "../components"; import StoryCard from "./components/StoryCard";

<Meta
    title="Button/Filled Button"
    component={FilledButton}
/>

export const Template = ({isLight, ...args}) => {
    return (
        <StoryCard isDark={isLight} isDense={!isLight}>
            <FilledButton isLight={isLight} {...args}>Button</FilledButton>
            <FilledButton isLight={isLight} isSmall {...args}>Button</FilledButton>
        </StoryCard>
    );
}

# Кнопка (Filled)

<Canvas>
    <Story name="Base">
        {Template.bind({})}
    </Story>
    <Story name="Colored" args={{isColored: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Disabled" args={{isDisabled: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Light" args={{isLight: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Light Disabled" args={{isLight: true, isDisabled: true}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={FilledButton} />

### Использование:

Для использования следует импортировать компонент `FilledButton` из `marketplace-ui-kit`.
<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {FilledButton} from "marketplace-ui-kit";
        ...
        <FilledButton>Button</FilledButton>
        <FilledButton isSmall >Button</FilledButton>
        <FilledButton isColored >Button</FilledButton>
        <FilledButton isDisabled >Button</FilledButton>
        <FilledButton isLight >Button</FilledButton>
  `}
/>
