import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import { Footer } from "../components";

<Meta title="Widget/Footer" component={Footer} />

export const Template = ({...args}) => {
    return (
        <div style={{minWidth: "1440px"}}>
            <Footer {...args} />
        </div>
    )
}

# Подвал

<Canvas>
    <Story name="Base">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Footer} />

### Использование:
Для использования следует импортировать компонент `Footer` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {Footer} from "marketplace-ui-kit";
        ...
        <Footer />
  `}
/>
