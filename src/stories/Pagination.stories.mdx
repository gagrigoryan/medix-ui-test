import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import { Pagination } from "../components";

<Meta title="Control/Pagination" component={Pagination} />

export const Template = ({...args}) => {
    return (
        <Pagination {...args} />
    )
}

# Пагинация

<Canvas isColumn={true}>
    <Story name="Base" args={{ totalPageCount: 7}}>
        {Template.bind({})}
    </Story>
    <Story name="Default page" args={{ totalPageCount: 6, currentPage: 3}}>
        {Template.bind({})}
    </Story>
    <Story name="More than 10 pages" args={{ totalPageCount: 16}}>
        {Template.bind({})}
    </Story>
    <Story name="More than 10 pages with default page" args={{ totalPageCount: 16, currentPage: 11}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Pagination} />

### Использование:
Для использования следует импортировать компонент `Pagination` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {Pagination} from "marketplace-ui-kit";
        ...
        <Pagination totalPageCount={5} />
        <Pagination totalPageCount={5} currentPage={3} />
        <Pagination
            totalPageCount={5}
            currentPage={3}
            onPageChange={(page) => {}}
        />
  `}
/>
