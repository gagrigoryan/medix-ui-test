import { Canvas, Meta, Story, Source, ArgsTable } from "@storybook/addon-docs/blocks";
import { InitialsAvatar } from "../components"; import StoryCard from "./components/StoryCard";

<Meta title="Initials Avatar" component={InitialsAvatar} />

export const Template = (args) => {
    return (
        <StoryCard isDark={args?.isLight}>
            <InitialsAvatar {...args} />
        </StoryCard>
    );
}

# Аватар с инициалами

<Canvas>
    <Story name="Base" args={{firstname: "David", lastname: "Smith"}}>
        {Template.bind({})}
    </Story>
    <Story name="Light" args={{firstname: "David", lastname: "Smith", isLight: true}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={InitialsAvatar} />

### Использование:
Для использования следует импортировать компонент `InitialsAvatar` из `marketplace-ui-kit`.

<Source
    language='tsx'
    dark
    format={true}
    code={`
        import {InitialsAvatar} from "marketplace-ui-kit";
        ...
        <InitialsAvatar firstname="Willard" lastname="Smith" />
        <InitialsAvatar firstname="Willard" lastname="Smith" isLight />
  `}
/>
